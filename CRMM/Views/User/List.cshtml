@using ModelCore
@model System.Collections.Generic.IList<DatabaseContext.Models.User>

@{
    ViewBag.Title = "Uživatelé";
    Layout = "_Layout";
    ViewData["Path"] = new Dictionary<string, string>() {
        { "CRMM", Url.Action("Index", "Home") },
        { "Uživatelé", "" }
    };
}

<h2>Uživatelé</h2>

@Html.PartialAsync("_Table", Model.Select(i => i as BaseModel).ToList()).Result